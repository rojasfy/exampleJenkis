pipeline {
    agent any

    stages {
     
        stage('Build') {
            steps {
               echo "etapa build no disponible"
            }
        }

        stage('Tests') {
            steps {
                  echo "etapa test no disponible"
            }
        }

        stage('deploy') {
            steps {
                  sh "docker-compose down -v"
            }
        }
       
    }
}
